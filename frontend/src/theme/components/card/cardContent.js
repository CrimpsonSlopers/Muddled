import pxToRem from "theme/functions/pxToRem";

const cardContent = {
    styleOverrides: {
        root: {
            marginTop: 0,
            marginBottom: 0,
            padding: `${pxToRem(24)}`,
        },
    },
};

export default cardContent;
